package main

func insert(intervals [][]int, newInterval []int) [][]int {
	res := make([][]int, 0, len(intervals))
	item := []int{intervals[0][0], intervals[0][1]}
	for i := 0; i < len(intervals); i++ {
		if 

	}

}
